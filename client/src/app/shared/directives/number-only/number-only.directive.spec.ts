import { NumberOnlyDirective } from "ali";

describe('NumberOnlyDirective', () => {
  it('should create an instance', () => {
    // const directive = new NumberOnlyDirective(2,4);
    // expect(directive).toBeTruthy();
  });
});
