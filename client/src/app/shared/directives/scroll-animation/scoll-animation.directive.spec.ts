import { ScollAnimationDirective } from './scoll-animation.directive';

describe('ScollAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ScollAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
